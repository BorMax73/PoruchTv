@model IEnumerable<User>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<div>
    @foreach (var user in Model)
    {
       @* <div>
            <img src="@user."/>
        </div>*@

        <a href="/user?name=@user.UserName">@user.UserName</a>
    }
</div>
